const state = {
    show:"block"
};

const getters = {
    getDisplay : (state)=> state.show
}

const actions={};

const mutations={};

export default{
    state,
    getters,
    actions,
    mutations
}